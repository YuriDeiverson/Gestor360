# URL do backend (pasta backend/). Sem barra no final.
# Celular na mesma rede: IPv4 do PC (ipconfig), mesma porta do backend (ex. 3002).
# Reinicie o Metro após mudar: npx expo start -c
EXPO_PUBLIC_API_URL=http://192.168.0.15:3002
